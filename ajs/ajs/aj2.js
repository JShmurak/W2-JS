//1. const sum = (x, y) => x + y;
/*2.
function d(kilograms){
return d * 1000;
}
console.log(d);

const e = function(kilograms){
    return e * 1000;
}
console.log(e);

//Difference between expression and declaration:
//declarations load first before the code is executed.
//expressions load only up to the line of code.

const af = kilograms => kilograms * 1000;*/


